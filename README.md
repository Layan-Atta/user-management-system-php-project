# 🚀 مشروع الأسبوع الثالث: نظام إدارة المستخدمين (PHP & MySQL)

هذا المشروع هو عبارة عن نظام ويب بسيط لإدارة المستخدمين مبني باستخدام PHP و MySQL مع واجهة مستخدم عصرية وتفاعلية.

---

## 🎯 الفكرة والهدف من المشروع

تطبيق المفهوم الأساسي للـ "Back-end" عن طريق إنشاء صفحة ويب ديناميكية تتفاعل مع قاعدة بيانات حقيقية. الهدف هو التدرب على العمليات الأربعة الأساسية في قواعد البيانات (CRUD) بشكل مبسط:

* **إنشاء (Create):** إضافة مستخدمين جدد إلى قاعدة البيانات عبر فورم (باستخدام `INSERT`).
* **قراءة (Read):** عرض جميع المستخدمين الموجودين في قاعدة البيانات داخل جدول (باستخدام `SELECT`).
* **تحديث (Update):** تغيير "حالة" المستخدم (نشط/غير نشط) عند الضغط على زر (باستخدام `UPDATE`).
* **(لم تُطبق) حذف (Delete):** (يمكن إضافتها كتطوير مستقبلي).

---

## 💻 التقنيات المستخدمة

* **PHP:** لغة البرمجة الأساسية للـ Back-end ومعالجة الطلبات.
* **MySQL:** نظام إدارة قواعد البيانات لتخزين بيانات المستخدمين.
* **HTML5:** لبناء هيكل الصفحة (الفورم والجدول).
* **CSS3:** لتصميم الواجهة بشكل احترافي وجذاب، بما في ذلك:
    * Flexbox / Grid لتنسيق العناصر.
    * Animations و Transitions لتفاعلية الواجهة.
    * تصميم متجاوب (Responsive Design) ليعمل على مختلف الشاشات.
* **XAMPP:** البيئة المحلية لتشغيل خادم Apache وقاعدة بيانات MySQL.

---

## 🔧 التحديات التي تمت مواجهتها

خلال تنفيذ المشروع، واجهت بعض التحديات التي تم التغلب عليها:

1.  **إعداد البيئة:** استغرق تشخيص خطأ `Fatal error: Maximum execution time exceeded` بعض الوقت، وتم حله عن طريق زيادة الوقت في ملف `php.ini`.
2.  **هيكلة قاعدة البيانات:** حدث لبس في البداية بين "قاعدة البيانات" و "الجدول"، مما أدى لإنشاء الجدول في مكان خاطئ. تم حل المشكلة بإنشاء قاعدة بيانات مستقلة وجدول بداخلها.
3.  **تحديث الحالة (Toggle):** تطلب الأمر فهماً جيداً لكيفية إرسال متغيرات `GET` في الرابط (`href`) لاستقبالها في كود PHP وتحديث الصف الصحيح في قاعدة البيانات.

---

## 📸 الناتج النهائي (Screenshots)

تم إنجاز المهمة بنجاح! لم يقتصر العمل على تحقيق الوظائف المطلوبة فقط، بل تم تصميم واجهة مستخدم (UI) عصرية وتجربة مستخدم (UX) محسّنة لجعل النظام ممتعاً وسهل الاستخدام.

### 1. الواجهة الرئيسية وقاعدة البيانات (phpMyAdmin) (قبل إضافة البيانات)


![قبل ويب](WebBefore.jpg)

![قبل بيانات](DbBefore.jpg)

### 2. إضافة مستخدم جديد


![بعد بيانات](WebAfter.jpg)

![بعد بيانات](DbAfter.jpg)





---

## ⚙️ كيفية تشغيل المشروع محلياً

1.  قم بتحميل ملف `index.php`.
2.  ضع الملف داخل مجلد `htdocs` في ملفات تثبيت `XAMPP`.
3.  قم بتشغيل `Apache` و `MySQL` من لوحة تحكم XAMPP.
4.  افتح `phpMyAdmin` وأنشئ قاعدة بيانات باسم `task_db`.
5.  بداخل `task_db`، أنشئ جدولاً باسم `users` بالمواصفات المطلوبة (id, name, age, status).
6.  افتح المتصفح وادخل الرابط: `http://localhost/index.php` (أو اسم المجلد إذا وضعته داخل مجلد).
